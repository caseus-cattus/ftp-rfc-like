#include <stdlib.h>

#define PI_PORT 6669
#define DTP_Port 6667


int init_sock(int port) {
    int sockfd;
    /* code */
    return sockfd;
}


int main(void) {
    int 
    pi_sockfd = init_sock(PI_PORT),
    dtp_sockfd = init_sock(DTP_Port);

    // code
    return 0;
}